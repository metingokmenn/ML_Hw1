"""
Metrics Module
Contains functions for calculating classification metrics.
"""

# This module will contain:
# - calculate_accuracy(y_true, y_pred) -> float
# - calculate_precision(y_true, y_pred) -> float
# - calculate_recall(y_true, y_pred) -> float
# - calculate_f_score(y_true, y_pred, beta=1) -> float
# - calculate_all_metrics(y_true, y_pred) -> dict

